# Filesystem Audit — 2026-08-15

Full-repo sweep of `/Users/latarencebutts/mdo3d` (~106MB, ~103MB in `projects/`) to identify files to remove, consolidate, or reorganize. Findings are split into **high-confidence operations** (safe to execute) and **low-confidence operations** (need a decision). Nothing in this document has been executed unless noted in the changelog at the bottom.

> **Validation pass (same day):** every high-confidence item was re-verified with direct diffs, checksums, and deployment-config inspection. See "Validation results & second-pass findings" below.

---

## Headline finding

The repo is **mid-migration and the migration was never committed**:

- On disk, `projects/blacklab/` was reorganized into `projects/divination/`, but git still tracks the old layout.
- All **1,837 pending git deletions** are exactly two committed `node_modules` trees (verified — zero non-node_modules deletions):
  - 721 files under `projects/blacklab/dreams/dream-interpreter/node_modules/`
  - 1,116 files under `projects/mdothree/landing/node_modules/`
- The 53 modified + 31 untracked files are the divination/Stripe/Firebase integration work, sitting unstaged since roughly April.
- A single Next.js build cache (`projects/jarvisbee/businessnames/business-name-generator/.next/`, 23MB) accounts for **22% of the entire repo**.

---

## High-confidence operations

### Remove (safe — regenerable or verified stale)

| # | Operation | Evidence |
|---|-----------|----------|
| 1 | **Commit the 1,837 node_modules deletions** | Every pending `D` in git status is one of the two tracked node_modules trees. Already deleted on disk; `node_modules/` already in `.gitignore`. Pure cleanup, zero content loss. |
| 2 | **Delete `projects/jarvisbee/businessnames/business-name-generator/.next/` (23MB)** | Next.js build cache; actual app source is ~100KB. Regenerated by `next build`. Also delete `__pycache__/` and `.pytest_cache/` in `projects/external/dailyaitoll/`. |
| 3 | **Delete the 18 `.DS_Store` files** | None are git-tracked; `.DS_Store` is already gitignored. Disk-only macOS junk. |
| 4 | **Delete `plugins/chrome-tea/`** | Completely empty directory. |
| 5 | **Delete `projects/blacklab/dreams/`, `oracle/`, `tarot/`** | Pre-migration copies. The `divination/` versions are unambiguously production: live Firebase configs (`oracle-mdo3d` project), Stripe payment links, `.vercel/` deployment metadata. Blacklab copies have placeholder credentials (`YOUR_FIREBASE_API_KEY`) and old URLs (`blacklabb.com/*`, `tarot-cards.vercel.app`). API code is identical in both; only frontends diverged, with divination ahead. |

**Keep** `projects/blacklab/landing/` and `projects/blacklab/research/` — these are the Blacklab B tech-tracker brand (`quantum.blacklabb.com`, `space.blacklabb.com`, `neuro.blacklabb.com`), unrelated to the divination migration and unique. `research/` contains three independent nested git repos (blacklab-neuro, blacklab-quantum, blacklab-space).

### Reorganize

| # | Operation | Evidence |
|---|-----------|----------|
| 6 | **Move root `logo1.png` / `logo2.png` into `logos/`** | Checksums confirm they are unique (not duplicates of anything in `logos/`), but brand assets belong together, not loose at repo root. |
| 7 | **Update `.gitignore`'s nested-repo list to match disk reality** | It still lists `projects/blacklab/astrology/...`, `projects/jarvisbee/...`, `projects/runwae/runwae` at pre-move paths. On disk these now live under `projects/divination/` and `projects/external/`. Stale entries mean nested repos at new paths are only invisible by accident. |
| 8 | **Commit the pending working-tree changes** | The 53 modified files plus new `stripeService.js` / `vercel.json` files under `projects/divination/*/api/` are finished Stripe/Firebase integration work, uncommitted since ~April — one bad `git checkout` from loss. |

---

## Low-confidence operations (need a decision)

### 1. Landing-page fragmentation (biggest structural question)

Five separate landing implementations exist:

- `landing/mdo3d-static` — tracked in main repo (index.html + guidance.html; both currently modified)
- `landing/mdo3d-compare` — nested git repo, untracked
- `landing/mdo3d-portfolio` — nested git repo, untracked (1.2MB, Next.js output)
- `projects/mdo3d/landing` — nested git repo, itself containing **two more nested repos** (`ronnascanner/mdothree-landing`, `ronnascanner/mdothree`)
- `projects/mdothree/landing` — tracked in main repo

Consolidating requires knowing which Vercel deployments are live. **Next step:** check Vercel CLI / deployment configs before touching any of these.

### 2. `projects/external/` — 56MB, over half the repo

`dailyaitoll` (23MB) and `runwae` (33MB) are fully independent git repos with their own histories and CI workflows. They arguably belong outside this repo entirely (e.g., `~/external/`). Moving them halves the repo footprint but changes paths any scripts may reference.

### 3. `documentation/` — stale since March

Largely superseded by the live `review/` dashboard (Jul 12) and root `STATUS.md` (Aug 13). Candidate split:

- **Keep:** `ARCHITECTURE.md`, `DOMAIN_MAPPING.md`
- **Archive:** `PROJECT_STATUS.md`, `PROJECTS_STATUS.md`, `DEVELOPMENT_STATUS.md`, `MASTER_SUMMARY.md`, `MDO3D_GAP_ANALYSIS.md`, `projects.md`
- **Verify first:** `STRIPE_*.md` (~33KB) — may be the only written record of the Stripe setup

### 4. `shared/` naming and overlap

- `shared/utils/` (a single numerology.js) vs `shared/utilities/` (Firebase config) — confusing naming; candidate rename `utils/` → `numerology/`.
- `shared/mdothree/` duplicates `404.html` / `account.html` / `pricing.html` with `projects/mdothree/mdothree-json/public/` — no clear source of truth.
- `shared/trauma/` holds two nested git repos (`api/`, `frontend/`) of unclear status — live or archived?

### 5. What to start tracking

Untracked operational assets that represent real work:

- `STATUS.md` (living status doc, updated Aug 13)
- `review/` (portfolio dashboard + voice QC, active)
- `loops/` specs (control plane; `loops/secrets/` correctly gitignored)
- `comms/` (voice guidelines scaffold)
- `social/` (designer handoff: brand system + post frames)
- `Organization/` (org model — just two READMEs: `README.md` + `Chief-of-Staff/README.md`; an earlier sub-report claiming it duplicated `loops/` control docs was **verified false**)

Tracking protects the work, but some may be untracked deliberately.

### 6. Git history rewrite

`.git` is 12MB, mostly the two node_modules trees. `git filter-repo` would shrink clones but rewrites history — only worth it if clone size matters.

### 7. `agents/`

A spec README with no code since February. Keep as placeholder, or fold its content into `Organization/`.

---

## Reference: verified facts

- Git-tracked file counts under `projects/`: blacklab 784, divination 93, mdothree 1,199 (1,116 of which are node_modules), mdo3d 4, ronnascanner 30.
- Directory sizes: `projects/external` 56M, `projects/jarvisbee` 23M (all in `.next/`), `projects/mdothree` 5.6M, `projects/leads` 5.6M (operational data — untracked is correct), `projects/rigor` 3.5M, `projects/divination` 3.1M, `projects/ronnascanner` 3.0M, `projects/mdo3d` 1.3M, `projects/blacklab` 1.2M.
- 45 nested git repos on disk; the `.gitignore` nested-repo list no longer matches their locations.
- `projects/leads/` vs `shared/leads/` — no duplication (full pipeline vs Firebase config only). `projects/rigor/` and `projects/ronnascanner/` are clean nested-repo containers (only `ronnascanner/resume-analyzer` is tracked in the main repo).
- Root `logo1.png` / `logo2.png` are unique files (MD5-checked against everything in `logos/`).

## Estimated impact of the high-confidence batch

- Disk: ~106MB → ~80MB
- Git status: 1,837 phantom deletions cleared; stranded migration work committed
- No live/production files touched

---

## Validation results & second-pass findings (2026-08-15, loop pass 2)

### Validations of high-confidence items — all confirmed

- **Blacklab deletions (item 5):** full recursive diff of `blacklab/{dreams,oracle,tarot}` vs `divination/` counterparts shows the *only* differing files are frontend `index.html` / `firebase.js` / `success.html` / `cancel.html` — exactly the branding/credential divergence where divination is ahead. Blacklab has **zero unique files** in those three subtrees (divination even has an extra `assets/` dir in oracle). Deletion is safe; the blacklab versions remain recoverable from git history.
- **node_modules deletions (item 1):** re-verified — 0 of the 1,837 pending deletions are anything other than the two node_modules trees.
- **jarvisbee 23MB (item 2):** confirmed 22.6MB is `.next/cache/webpack/*` pack files. Regenerable.
- **Secrets hygiene:** repo-wide scan found only `.env.example` templates — no live `.env`, `.pem`, or credential files on disk outside gitignored areas.

### Landing-page question — now mostly resolved

Vercel `project.json` inspection + title/mtime comparison shows the truth:

| Directory | Vercel project | Verdict |
|---|---|---|
| `landing/mdo3d-static` | `mdo3d-static` | **Canonical.** Same site as the two below ("MDO3D — AI Platform Ecosystem"), newest content (May 10 + current uncommitted theme-toggle work). |
| `projects/mdo3d/guidance/mdo3d` | `mdo3d-landing` | Stale iteration of the same site (Mar 25). Archive/delete candidate after confirming which Vercel project serves the live domain. |
| `projects/mdo3d/landing` | **`rigor`** (!) | Stale iteration (Mar 24) whose `.vercel` link points at the *rigor* Vercel project — a stale/wrong link. Archive/delete candidate. |
| `landing/mdo3d-compare` | `mdo3d-compare` | Distinct site (comparison page). Keep. |
| `landing/mdo3d-portfolio` | `mdo3d-portfolio` | Distinct site (portfolio). Keep. Contains regenerable `out/` (908K). |
| `projects/mdothree/landing` | `landing` | Distinct site (mdothree utilities landing). Keep. |

Remaining unknown: which Vercel project the live domain(s) actually point to — confirm in the Vercel dashboard before deleting the two stale `projects/mdo3d/` copies.

### New findings from the checksum duplicate scan

1. **Root `logo2.png` is a duplicate** — byte-identical to `social/assets/m3-mono-light.png` and `social/brand/brand-assets/logos/logo2.png`. Delete the root copy (upgrade from "move to logos/"). `logo1.png` has no duplicate anywhere → move it to `logos/` as originally planned.
2. **Deliberate copy-pattern for shared UI** (informational, not junk): `styles.css` is byte-identical across 8 locations (5 `mdothree-*` repos, 2 `ronnascanner-*` repos, and `shared/mdothree/styles.css`); `stripe-paywall.js` identical across 5 `mdothree-*` repos; `cardDatabase.js` identical across 3 plugins + oracle. This confirms `shared/mdothree/` is the de-facto source of truth, copied into each independently-deployed nested repo. Recommendation: document this in `shared/README.md` and/or add a sync script — don't dedupe the copies themselves (each Vercel deploy needs its own copy).
3. **`projects/leads/data/20260403_corporate.csv` ≡ `projects/leads/output/all_corporate_20260403.csv`** (408K duplicate data file) — one can go.
4. **`projects/external/runwae/runwae/__MACOSX/`** — recursively nested zip-extraction junk (only 8KB, but pure garbage; lives inside the nested runwae repo). runwae's 33MB is otherwise legitimate assets (icons/img/fonts/guides).
5. **`__pycache__` beyond dailyaitoll:** also in `review/`, `projects/leads/src`, `projects/leads/web/backend`, `projects/leads/cron.d` (~300K total). All regenerable; candidates for a global `__pycache__/` gitignore entry.
6. **`dailyaitoll/data/daily_rollup.json` ≡ `dailyaitoll/web/data/daily_rollup.json`** — likely a deliberate publish copy; leave to that repo.

## Changelog

- **2026-08-15** — Audit performed and documented. No operations executed yet.
- **2026-08-15 (pass 2)** — All high-confidence items validated (diffs, checksums, Vercel configs). Landing-page fragmentation resolved to a canonical + 2-stale + 3-distinct map. Root `logo2.png` reclassified from "move" to "delete (duplicate)". New removals added: leads duplicate CSV, extra `__pycache__` dirs, runwae `__MACOSX/`. Still nothing executed.
- **2026-08-15 (executed)** — All high-confidence operations completed:
  - Disk: deleted jarvisbee `.next` (23MB), all `__pycache__`/`.pytest_cache`, runwae `__MACOSX/`, `plugins/chrome-tea/`, 18 `.DS_Store`, root `logo2.png` (dup), leads duplicate CSV; moved `logo1.png` → `logos/`. Repo ~106MB → ~82MB content.
  - Commit `50d3e06`: removed 1,837 tracked node_modules files + stale `blacklab/{dreams,oracle,tarot}` copies; fixed `.gitignore` nested-repo paths.
  - Commit `250fd91`: committed the stranded divination Stripe/Firebase integration work (62 files; secrets-scanned — all Stripe keys via env vars, Firebase keys are public client config).
  - Remaining untracked: operational dirs (`review/`, `loops/`, `social/`, `comms/`, `Organization/`, `scripts/`, `STATUS.md`, `shared/` config dirs, `projects/leads/`, `projects/external/`) — tracking decision still open (low-confidence item 5). Three divination nested repos (`astrology`, `iching`, `runes`) have uncommitted changes *inside* them.
